# VAPID Keys for Web Push Notifications
# Generate these using: npm run generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PRIVATE_KEY=your_private_key_here
VAPID_SUBJECT=mailto:your-email@example.com

# Hostname Configuration
# Set this to your Tailscale hostname (e.g., your-machine.tailnet-name.ts.net)
# or local IP address for SSL certificate generation and display
APP_HOSTNAME=your-machine.tailnet-name.ts.net

# API Server Configuration
# Set this to point to the standalone API server (port 3109)
# Replace hostname with your APP_HOSTNAME value
NEXT_PUBLIC_API_URL=https://your-machine.tailnet-name.ts.net:3109
API_PORT=3109
